(()=>{
if(window.__pulseMtfSnapshotsV5)return;window.__pulseMtfSnapshotsV5=true;
const TFS=['15m','1h','4h','1d'],AN={'15m':240,'1h':240,'4h':200,'1d':200},PRE={short:80,standard:140,long:200};let preset=localStorage.getItem('pulseSnapPreset')||'standard',activeTF=window.__pulseActiveTF||'4h';
const $=id=>document.getElementById(id),num=v=>{v=Number(v);return Number.isFinite(v)?v:null},fmt=v=>Number(v).toLocaleString(undefined,{maximumFractionDigits:2});
function symbol(){return($('symbol')?.value||window.symbol||new URLSearchParams(location.search).get('symbol')||'BTCUSDT').toUpperCase().replace(/[^A-Z0-9]/g,'')}
async function get(u){const r=await fetch(u,{cache:'no-store'}),j=await r.json();if(!r.ok||!j?.ok)throw Error(j?.error||`HTTP ${r.status}`);return j}
function setTF(tf){if(!TFS.includes(tf))return;activeTF=tf;window.__pulseActiveTF=tf;document.querySelectorAll('.mtfs-tab').forEach(x=>x.classList.toggle('on',x.dataset.tf===tf));window.dispatchEvent(new CustomEvent('pulse:tfchange',{detail:{tf}}))}
function ensure(){if($('mtfSnapshotV1'))return true;const host=document.querySelector('main,.wrap,.workspace,.container,.app')||document.body,sec=document.createElement('section');sec.id='mtfSnapshotV1';sec.innerHTML=`<div class="mh"><div><b>MTF 분석 스냅샷</b><small>구조 / 패턴 · TF 동기화</small></div><button id="mtfsRefresh">분석 갱신</button></div><div class="mp"><span>표시 범위</span><button data-p="short">짧게 80</button><button data-p="standard">표준 140</button><button data-p="long">길게 200</button></div><div id="mtfsStatus">대기</div><div class="mtfs-tabs">${TFS.map(tf=>`<button class="mtfs-tab" data-tf="${tf}">${tf}</button>`).join('')}</div><div class="mg">${TFS.map(tf=>`<article class="mtfs-card" data-tf="${tf}"><div class="mt"><b>${tf}</b><span id="mtfsMeta-${tf}">-</span></div><div id="mtfsBadges-${tf}" class="mb"></div><canvas id="mtfs-${tf}" width="900" height="520"></canvas></article>`).join('')}</div>`;host.appendChild(sec);const st=document.createElement('style');st.textContent=`#mtfSnapshotV1{margin:12px 0;padding:12px;border:1px solid #24364d;border-radius:16px;background:#09131f;color:#e8f0fb}.mh,.mt,.mp{display:flex;align-items:center;gap:7px}.mh,.mt{justify-content:space-between}.mh small,#mtfsStatus,.mt span,.mp{color:#8296ad;font-size:10px}.mp{margin:8px 0;flex-wrap:wrap}.mp button,.mtfs-tab{background:#0a1624;color:#9ab0c9;border:1px solid #263c57;border-radius:999px;padding:5px 9px}.mp button.on,.mtfs-tab.on{background:#153a67;color:white}.mtfs-tabs,.mb{display:flex;gap:5px;flex-wrap:wrap}.mg{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:10px;margin-top:8px}.mtfs-card{border:1px solid #22364d;border-radius:13px;background:#07111b;overflow:hidden}.mt,.mb{padding:8px 10px}.mb{font-size:9px;color:#ffca73}.mtfs-card canvas{display:block;width:100%;height:auto}@media(max-width:760px){#mtfSnapshotV1{margin-top:calc(12px + env(safe-area-inset-top))}.mg{display:flex;overflow-x:auto}.mtfs-card{min-width:92%}}`;document.head.appendChild(st);$('mtfsRefresh').onclick=()=>refresh(true);document.querySelectorAll('.mtfs-tab').forEach(b=>b.onclick=()=>setTF(b.dataset.tf));document.querySelectorAll('.mp button').forEach(b=>{b.classList.toggle('on',b.dataset.p===preset);b.onclick=()=>{preset=b.dataset.p;localStorage.setItem('pulseSnapPreset',preset);document.querySelectorAll('.mp button').forEach(x=>x.classList.toggle('on',x===b));refresh(true)}});setTF(activeTF);return true}
function draw(tf,d,pd){const cv=$(`mtfs-${tf}`),ctx=cv?.getContext('2d');if(!ctx)return;const full=d.candles||[],N=PRE[preset],c=full.slice(-N),off=full.length-c.length;if(!c.length)return;const W=cv.width,H=cv.height,p={l:48,r:86,t:40,b:30};let lo=Math.min(...c.map(k=>+k.low)),hi=Math.max(...c.map(k=>+k.high)),sp=hi-lo||1;lo-=sp*.06;hi+=sp*.06;const x=i=>p.l+i*(W-p.l-p.r)/Math.max(1,c.length-1),y=v=>p.t+(hi-v)/(hi-lo)*(H-p.t-p.b);ctx.fillStyle='#07111b';ctx.fillRect(0,0,W,H);ctx.strokeStyle='#1a2a3d';for(let g=0;g<5;g++){const yy=p.t+g*(H-p.t-p.b)/4;ctx.beginPath();ctx.moveTo(p.l,yy);ctx.lineTo(W-p.r,yy);ctx.stroke();ctx.fillStyle='#8096af';ctx.font='11px system-ui';ctx.fillText(fmt(hi-g*(hi-lo)/4),W-p.r+5,yy+4)}for(let i=0;i<c.length;i++){const k=c[i],xx=x(i),up=+k.close>=+k.open;ctx.strokeStyle=ctx.fillStyle=up?'#35d69a':'#ff6b7a';ctx.beginPath();ctx.moveTo(xx,y(+k.low));ctx.lineTo(xx,y(+k.high));ctx.stroke();const a=y(Math.max(+k.open,+k.close)),b=y(Math.min(+k.open,+k.close));ctx.fillRect(xx-2,a,4,Math.max(1,b-a))}
function structural(L,color){const pts=(L?.touchPoints||[]).map(q=>({i:+q.barIndex,p:num(q.price)})).filter(q=>q.i>=off&&q.i<full.length&&q.p!=null);if(pts.length<2)return;ctx.save();ctx.globalAlpha=.5;ctx.strokeStyle=color;ctx.setLineDash([3,5]);ctx.beginPath();ctx.moveTo(x(pts[0].i-off),y(pts[0].p));ctx.lineTo(x(pts.at(-1).i-off),y(pts.at(-1).p));ctx.stroke();ctx.restore()}structural(d.trendlines?.support,'#55dfb4');structural(d.trendlines?.resistance,'#ff7480');
const pats=(pd?.patterns||[]).filter(q=>String(q.state||'').toUpperCase()!=='FAILED').slice(0,2);function patternLine(L,q){if(!L||!Number.isFinite(+L.slope)||!Number.isFinite(+L.intercept))return;const anchors=(q.anchors||[]).map(a=>+a.index).filter(Number.isFinite),start=Math.max(off,+L.startIndex||Math.min(...anchors,off)),apex=num(q.apex?.index??q.apex?.x),last=anchors.length?Math.max(...anchors):full.length-1,end=Math.min(full.length-1,+L.endIndex||last,apex??last);if(end<=start)return;ctx.strokeStyle='#ffca73';ctx.lineWidth=2.4;ctx.beginPath();ctx.moveTo(x(start-off),y(+L.slope*start + +L.intercept));ctx.lineTo(x(end-off),y(+L.slope*end + +L.intercept));ctx.stroke()}pats.forEach((q,n)=>{patternLine(q.upperLine,q);patternLine(q.lowerLine,q);ctx.fillStyle='#ffca73';ctx.font='bold 11px system-ui';ctx.fillText(`${q.type} ${Math.round(q.confidence||0)}%`,p.l+8,p.t+16+n*15)});
for(const e of(d.events||[]).slice(-10)){const i=+e.i,v=num(e.level);if(i<off||i>=full.length||v==null)continue;ctx.fillStyle=e.dir==='up'?'#35d69a':'#ff6b7a';ctx.font='bold 10px system-ui';ctx.fillText(e.type,x(i-off)+3,y(v)+(e.dir==='up'?-14:18))}ctx.fillStyle='#dce8f7';ctx.font='bold 15px system-ui';ctx.fillText(`${d.symbol||symbol()} · ${tf}`,p.l,19);ctx.fillStyle='#8096af';ctx.font='10px system-ui';ctx.fillText(`표시 ${N}봉 · 분석 ${AN[tf]}봉 · 점선=장기 구조 · 실선=패턴`,p.l,H-8);$(`mtfsBadges-${tf}`).textContent=pats.length?pats.map(q=>`${q.type} ${Math.round(q.confidence||0)}%`).join(' · '):'패턴 없음'}
let busy=false,last='';async function refresh(force=false){if(busy||!ensure())return;const s=symbol(),key=s+'|'+preset;if(!force&&key===last)return;busy=true;$('mtfsStatus').textContent=`${s} 분석 중...`;try{const out=await Promise.allSettled(TFS.map(async tf=>{const[d,p]=await Promise.all([get(`/api/structure?symbol=${s}&interval=${tf}&limit=${Math.max(AN[tf],PRE[preset])}&_=${Date.now()}`),get(`/api/pattern?symbol=${s}&interval=${tf}&limit=${AN[tf]}&_=${Date.now()}`)]);return{d,p}}));out.forEach((r,i)=>{const tf=TFS[i];if(r.status==='fulfilled'){draw(tf,r.value.d,r.value.p);$(`mtfsMeta-${tf}`).textContent=`${PRE[preset]} / ${AN[tf]}봉`}else $(`mtfsMeta-${tf}`).textContent='오류'});last=key;$('mtfsStatus').textContent=`${s} · TF 동기화 완료`}finally{busy=false}}
let n=0,t=setInterval(()=>{if(ensure())setTimeout(()=>refresh(false),250);if(++n>40)clearInterval(t)},300);window.addEventListener('pulse:tfchange',e=>{if(TFS.includes(e.detail?.tf)){activeTF=e.detail.tf;document.querySelectorAll('.mtfs-tab').forEach(x=>x.classList.toggle('on',x.dataset.tf===activeTF))}});document.addEventListener('change',e=>{if(e.target?.id==='symbol')setTimeout(()=>refresh(true),350)});
})();