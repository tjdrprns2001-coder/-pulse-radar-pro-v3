(()=>{
  if(window.__pulseMtfSnapshotsV2)return;window.__pulseMtfSnapshotsV2=true;
  const TFS=['15m','1h','4h','1d'];
  const COLORS={bg:'#07111b',grid:'#1a2a3d',up:'#35d69a',down:'#ff6b7a',text:'#dce8f7',muted:'#8096af',support:'#55dfb4',resist:'#ff7480',fvg:'rgba(159,122,234,.18)',ob:'rgba(255,190,80,.16)',liq:'rgba(64,178,255,.15)',cloudUp:'rgba(53,214,154,.09)',cloudDn:'rgba(255,107,122,.08)'};
  const $=id=>document.getElementById(id),num=v=>{v=Number(v);return Number.isFinite(v)?v:null};
  function symbol(){return (($('symbol')?.value)||window.symbol||new URLSearchParams(location.search).get('symbol')||'BTCUSDT').toUpperCase().replace(/[^A-Z0-9]/g,'')}
  async function jget(u){const r=await fetch(u,{cache:'no-store',headers:{Accept:'application/json'}}),t=await r.text();let j;try{j=JSON.parse(t)}catch{throw Error('응답 형식 오류')}if(!r.ok||!j?.ok)throw Error(j?.error||`HTTP ${r.status}`);return j}
  function ensureUI(){
    if($('mtfSnapshotV1'))return true;
    const host=document.querySelector('main,.wrap,.workspace,.container,.app')||document.body;if(!host)return false;
    const sec=document.createElement('section');sec.id='mtfSnapshotV1';sec.innerHTML=`<div class="mtfs-head"><div><b>MTF 분석 스냅샷</b><small>15m · 1h · 4h · 1d 고정 140봉 · 탭해서 확대</small></div><button id="mtfsRefresh">분석 갱신</button></div><div id="mtfsStatus">대기</div><div class="mtfs-tabs">${TFS.map((tf,i)=>`<button class="mtfs-tab${i===0?' on':''}" data-tf="${tf}">${tf}</button>`).join('')}</div><div class="mtfs-grid">${TFS.map(tf=>`<article class="mtfs-card" data-tf="${tf}"><div class="mtfs-title"><b>${tf}</b><span id="mtfsMeta-${tf}">-</span></div><div class="mtfs-badges" id="mtfsBadges-${tf}"></div><canvas id="mtfs-${tf}" width="900" height="520"></canvas></article>`).join('')}</div><div id="mtfsZoom" class="mtfs-zoom"><button id="mtfsZoomClose">닫기</button><div id="mtfsZoomTitle"></div><img id="mtfsZoomImg" alt="MTF snapshot"></div>`;
    const firstChart=document.querySelector('#chartBox,.chart-card,.chartPanel,.price-panel,.chart-wrap,canvas'),anchor=firstChart?.closest?.('section,.card,.panel,.box,div');if(anchor?.parentNode)anchor.parentNode.insertBefore(sec,anchor.nextSibling);else host.appendChild(sec);
    const st=document.createElement('style');st.id='mtfSnapshotV2Style';st.textContent=`#mtfSnapshotV1{margin:12px 0;padding:12px;border:1px solid #24364d;border-radius:16px;background:#09131f;color:#e8f0fb}.mtfs-head{display:flex;align-items:center;justify-content:space-between;gap:10px}.mtfs-head b{font-size:18px}.mtfs-head small{display:block;color:#8296ad;font-size:11px;margin-top:3px}#mtfsRefresh{background:#0d5fc9;color:white;border:1px solid #3b82f6;border-radius:10px;padding:9px 13px;font-weight:700}#mtfsStatus{font-size:11px;color:#8296ad;margin:7px 0}.mtfs-tabs{display:flex;gap:6px;margin:6px 0 9px}.mtfs-tab{background:#0a1624;color:#9ab0c9;border:1px solid #263c57;border-radius:999px;padding:6px 11px;font-weight:700}.mtfs-tab.on{background:#153a67;color:#fff;border-color:#3b82f6}.mtfs-grid{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:10px}.mtfs-card{border:1px solid #22364d;border-radius:13px;background:#07111b;overflow:hidden;cursor:zoom-in}.mtfs-title{display:flex;justify-content:space-between;align-items:center;padding:9px 10px 4px;color:#e7eef8}.mtfs-title span{font-size:10px;color:#8296ad}.mtfs-badges{display:flex;gap:5px;flex-wrap:wrap;padding:0 10px 7px}.mtfs-badge{font-size:9px;border:1px solid #314760;border-radius:999px;padding:3px 6px;color:#9fb4ca}.mtfs-badge.ok{color:#5de2b7;border-color:#276d5b}.mtfs-badge.warn{color:#ffca73;border-color:#765c2c}.mtfs-card canvas{display:block;width:100%;height:auto;background:#07111b}.mtfs-zoom{display:none;position:fixed;inset:0;z-index:999999;background:rgba(2,7,13,.96);padding:env(safe-area-inset-top,12px) 10px 20px;align-items:center;justify-content:center;flex-direction:column;gap:10px}.mtfs-zoom.open{display:flex}.mtfs-zoom img{max-width:100%;max-height:82vh;border:1px solid #2c4059;border-radius:12px;background:#07111b}.mtfs-zoom button{align-self:flex-end;background:#16283b;color:#fff;border:1px solid #36506e;border-radius:9px;padding:8px 12px}.mtfs-zoom #mtfsZoomTitle{font-weight:800}@media(max-width:760px){.mtfs-grid{display:flex;overflow-x:auto;scroll-snap-type:x mandatory;padding-bottom:4px}.mtfs-card{min-width:92%;scroll-snap-align:start}.mtfs-head b{font-size:16px}.mtfs-tabs{position:sticky;top:0;z-index:4;background:#09131f;padding:4px 0}}`;document.head.appendChild(st);
    $('mtfsRefresh').onclick=()=>refresh(true);$('mtfsZoomClose').onclick=closeZoom;$('mtfsZoom').onclick=e=>{if(e.target.id==='mtfsZoom')closeZoom()};
    document.querySelectorAll('.mtfs-card').forEach(c=>c.onclick=()=>openZoom(c.dataset.tf));
    document.querySelectorAll('.mtfs-tab').forEach(b=>b.onclick=()=>scrollToTf(b.dataset.tf));
    return true;
  }
  function scrollToTf(tf){document.querySelectorAll('.mtfs-tab').forEach(b=>b.classList.toggle('on',b.dataset.tf===tf));document.querySelector(`.mtfs-card[data-tf="${tf}"]`)?.scrollIntoView({behavior:'smooth',block:'nearest',inline:'center'})}
  function openZoom(tf){const cv=$(`mtfs-${tf}`);if(!cv)return;$('mtfsZoomImg').src=cv.toDataURL('image/png');$('mtfsZoomTitle').textContent=`${symbol()} · ${tf} 분석 스냅샷`;$('mtfsZoom').classList.add('open')}
  function closeZoom(){$('mtfsZoom')?.classList.remove('open')}
  function ichimoku(c){const mid=(arr,i,n)=>{if(i<n-1)return null;let hi=-Infinity,lo=Infinity;for(let k=i-n+1;k<=i;k++){hi=Math.max(hi,+arr[k].high);lo=Math.min(lo,+arr[k].low)}return(hi+lo)/2};return c.map((_,i)=>{const ten=mid(c,i,9),kij=mid(c,i,26),a=ten!=null&&kij!=null?(ten+kij)/2:null,b=mid(c,i,52);return{a,b}})}
  function zKind(z){const s=String(z?.type||z?.kind||z?.label||z?.name||'').toLowerCase();if(s.includes('fvg'))return'fvg';if(s.includes('order')||s==='ob'||s.includes('ob '))return'ob';if(s.includes('liq')||s.includes('liquid'))return'liq';if(s.includes('support'))return'support';if(s.includes('resist'))return'resistance';return z?.type==='support'?'support':z?.type==='resistance'?'resistance':'zone'}
  function draw(tf,d,patternData){
    const cv=$(`mtfs-${tf}`);if(!cv)return;const ctx=cv.getContext('2d'),W=cv.width,H=cv.height,p={l:48,r:64,t:30,b:30};
    const full=d.candles||[],c=full.slice(-140),off=full.length-c.length;if(!c.length)return;
    let min=Math.min(...c.map(x=>+x.low)),max=Math.max(...c.map(x=>+x.high));const zones=d.zones||[];for(const z of zones){if(num(z.low)!=null)min=Math.min(min,+z.low);if(num(z.high)!=null)max=Math.max(max,+z.high)}const span=Math.max(1e-9,max-min);min-=span*.05;max+=span*.05;
    const x=i=>p.l+i*(W-p.l-p.r)/Math.max(1,c.length-1),y=v=>p.t+(max-v)/(max-min)*(H-p.t-p.b);
    ctx.clearRect(0,0,W,H);ctx.fillStyle=COLORS.bg;ctx.fillRect(0,0,W,H);ctx.strokeStyle=COLORS.grid;ctx.lineWidth=1;
    for(let g=0;g<5;g++){const yy=p.t+g*(H-p.t-p.b)/4;ctx.beginPath();ctx.moveTo(p.l,yy);ctx.lineTo(W-p.r,yy);ctx.stroke();const pr=max-g*(max-min)/4;ctx.fillStyle=COLORS.muted;ctx.font='11px system-ui';ctx.fillText(pr>=100?pr.toFixed(0):pr.toPrecision(5),W-p.r+5,yy+4)}
    const ichi=ichimoku(c);for(let i=1;i<c.length;i++){const q=ichi[i-1],r=ichi[i];if(q.a==null||q.b==null||r.a==null||r.b==null)continue;ctx.fillStyle=((q.a+q.b+r.a+r.b)/4 >= (c[i].close+c[i-1].close)/2)?COLORS.cloudDn:COLORS.cloudUp;ctx.beginPath();ctx.moveTo(x(i-1),y(q.a));ctx.lineTo(x(i),y(r.a));ctx.lineTo(x(i),y(r.b));ctx.lineTo(x(i-1),y(q.b));ctx.closePath();ctx.fill()}
    for(const z of zones.slice(0,12)){const lo=num(z.low),hi=num(z.high);if(lo==null||hi==null)continue;const k=zKind(z);ctx.fillStyle=k==='fvg'?COLORS.fvg:k==='ob'?COLORS.ob:k==='liq'?COLORS.liq:k==='support'?'rgba(53,214,154,.09)':'rgba(255,107,122,.08)';ctx.fillRect(p.l,y(hi),W-p.l-p.r,Math.max(2,y(lo)-y(hi)));if(['fvg','ob','liq'].includes(k)){ctx.fillStyle=COLORS.muted;ctx.font='9px system-ui';ctx.fillText(k.toUpperCase(),p.l+4,y(hi)+11)}}
    for(let i=0;i<c.length;i++){const k=c[i],xx=x(i),up=+k.close>=+k.open;ctx.strokeStyle=ctx.fillStyle=up?COLORS.up:COLORS.down;ctx.beginPath();ctx.moveTo(xx,y(+k.low));ctx.lineTo(xx,y(+k.high));ctx.stroke();const yo=y(Math.max(+k.open,+k.close)),yc=y(Math.min(+k.open,+k.close));ctx.fillRect(xx-2.2,yo,4.4,Math.max(1,yc-yo))}
    function line(L,color){if(!L)return;const raw=Array.isArray(L.touchPoints)?L.touchPoints:[];const pts=raw.map(q=>({i:Number(q.barIndex),p:num(q.price)})).filter(q=>Number.isFinite(q.i)&&q.p!=null&&q.i>=off&&q.i<full.length);if(pts.length<2)return;const a=pts[0],b=pts.at(-1),ia=a.i-off,ib=b.i-off;if(ib<=ia)return;ctx.strokeStyle=color;ctx.lineWidth=3;ctx.beginPath();ctx.moveTo(x(ia),y(a.p));ctx.lineTo(x(ib),y(b.p));ctx.stroke();for(const q of pts){ctx.fillStyle=color;ctx.beginPath();ctx.arc(x(q.i-off),y(q.p),3.5,0,Math.PI*2);ctx.fill()}}
    line(d.trendlines?.support,COLORS.support);line(d.trendlines?.resistance,COLORS.resist);
    for(const e of (d.events||[]).slice(-14)){const gi=Number(e.i),lvl=num(e.level);if(!Number.isFinite(gi)||lvl==null||gi<off||gi>=full.length)continue;ctx.fillStyle=e.dir==='up'?COLORS.up:COLORS.down;ctx.font='bold 11px system-ui';ctx.fillText(e.type,x(gi-off)+3,y(lvl)+(e.dir==='up'?-7:14))}
    const pat=patternData?.pattern;const state=String(pat?.state||pat?.status||'').toUpperCase(),conf=num(pat?.confidence??pat?.score);if(pat&&state&&state!=='FAILED'){ctx.fillStyle='#ffca73';ctx.font='bold 12px system-ui';ctx.fillText(`Falling Wedge · ${state}${conf!=null?' · '+Math.round(conf)+'%':''}`,p.l+220,18)}
    ctx.fillStyle=COLORS.text;ctx.font='bold 15px system-ui';ctx.fillText(`${d.symbol||symbol()} · ${tf}`,p.l,18);ctx.fillStyle=COLORS.muted;ctx.font='10px system-ui';ctx.fillText('고정 140봉 · Trendline/BOS/CHoCH/FVG/OB/Liquidity/Ichimoku',p.l,H-8);
    const badges=$(`mtfsBadges-${tf}`);if(badges){const s=d.trendlines?.support?'S 추세선':'S 없음',r=d.trendlines?.resistance?'R 추세선':'R 없음',ptext=pat&&state&&state!=='FAILED'?`Wedge ${state}${conf!=null?' '+Math.round(conf)+'%':''}`:'확정 패턴 없음';badges.innerHTML=`<span class="mtfs-badge ${d.trendlines?.support?'ok':''}">${s}</span><span class="mtfs-badge ${d.trendlines?.resistance?'ok':''}">${r}</span><span class="mtfs-badge ${pat&&state&&state!=='FAILED'?'warn':''}">${ptext}</span>`}
  }
  let busy=false,lastSym='';
  async function refresh(force=false){if(busy||!ensureUI())return;const s=symbol();if(!force&&s===lastSym)return;busy=true;$('mtfsStatus').textContent=`${s} 스냅샷 생성 중...`;try{const jobs=TFS.map(async tf=>{const stamp=Date.now(),[structure,pattern]=await Promise.allSettled([jget(`/api/structure?symbol=${encodeURIComponent(s)}&interval=${tf}&limit=500&_=${stamp}`),jget(`/api/pattern?symbol=${encodeURIComponent(s)}&interval=${tf}&limit=240&_=${stamp}`)]);if(structure.status!=='fulfilled')throw structure.reason;return{tf,d:structure.value,pattern:pattern.status==='fulfilled'?pattern.value:null}});const out=await Promise.allSettled(jobs);out.forEach((r,i)=>{const tf=TFS[i],m=$(`mtfsMeta-${tf}`);if(r.status==='fulfilled'){draw(tf,r.value.d,r.value.pattern);if(m)m.textContent=new Date().toLocaleTimeString('ko-KR',{hour:'2-digit',minute:'2-digit'})+' 생성'}else{if(m)m.textContent='오류';const cv=$(`mtfs-${tf}`);if(cv){const x=cv.getContext('2d');x.fillStyle=COLORS.bg;x.fillRect(0,0,cv.width,cv.height);x.fillStyle=COLORS.down;x.font='16px system-ui';x.fillText('스냅샷 생성 실패',40,60)}}});lastSym=s;$('mtfsStatus').textContent=`${s} · MTF 4개 스냅샷 완료`}finally{busy=false}}
  function install(){if(!ensureUI())return false;setTimeout(()=>refresh(false),350);return true}
  let n=0,t=setInterval(()=>{if(install()&&++n>12)clearInterval(t);else if(++n>80)clearInterval(t)},300);
  document.addEventListener('change',e=>{if(e.target?.id==='symbol')setTimeout(()=>refresh(true),500)});
})();