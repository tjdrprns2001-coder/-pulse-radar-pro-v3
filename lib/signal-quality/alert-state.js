(function(root,factory){const api=factory();if(typeof module==='object'&&module.exports)module.exports=api;if(root)root.PulseAlertState=api;})(typeof globalThis!=='undefined'?globalThis:this,function(){
const STATES=Object.freeze(['OBSERVE','WATCH','ARMED','TRIGGERED','CONFIRMED','INVALIDATED','EXPIRED','NO_SIGNAL']);
function nextAlertState(prev='OBSERVE',x={}){const p=STATES.includes(prev)?prev:'OBSERVE';if(x.expired)return'EXPIRED';if(x.invalidated)return'INVALIDATED';if(x.integrityState&&['stale','failed','reconnecting','backfill','verifying'].includes(x.integrityState))return'NO_SIGNAL';const met=Math.max(0,Number(x.conditionsMet)||0),total=Math.max(1,Number(x.conditionsTotal)||3),ratio=met/total;if(x.confirmed&&ratio>=.8)return'CONFIRMED';if(x.triggered&&ratio>=.67)return'TRIGGERED';if(ratio>=.8)return'ARMED';if(ratio>=.34)return'WATCH';if(p==='ARMED'&&ratio>=.5)return'ARMED';if(p==='WATCH'&&ratio>=.2)return'WATCH';return'OBSERVE'}
function transition(prev,input={},nowMs=Date.now()){const state=nextAlertState(prev?.state||prev||'OBSERVE',input),changed=state!==(prev?.state||prev);return{state,changed,firstSeenAt:prev?.firstSeenAt||nowMs,lastStateChangeAt:changed?nowMs:(prev?.lastStateChangeAt||nowMs),maxSeveritySeen:Math.max(Number(prev?.maxSeveritySeen)||0,Number(input.severity)||0),conditionsMet:Number(input.conditionsMet)||0,conditionsTotal:Number(input.conditionsTotal)||3}}
return{STATES,nextAlertState,transition};
});
