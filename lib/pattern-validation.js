const {detectChartPatterns,ENGINE_VERSION,CALIBRATION_VERSION}=require('./chart-patterns.js');
const {analyzeFallingWedge}=require('./pattern-engine.js');
const {tunablesFor}=require('./calibration-profile.js');
const avg=a=>a.length?a.reduce((s,x)=>s+x,0)/a.length:null;
const med=a=>{if(!a.length)return null;const b=[...a].sort((x,y)=>x-y),m=Math.floor(b.length/2);return b.length%2?b[m]:(b[m-1]+b[m])/2};
const clamp=(v,a=0,b=100)=>Math.max(a,Math.min(b,v));
const DIR={falling_wedge:1,ascending_triangle:1,descending_triangle:-1,symmetrical_triangle:0,ascending_channel:1,descending_channel:-1,head_and_shoulders:-1,inverse_head_and_shoulders:1,double_top:-1,double_bottom:1};
function pivots(k,left=3,right=3){const out=[];for(let i=left;i<k.length-right;i++){let H=1,L=1;for(let j=i-left;j<=i+right;j++){if(j===i)continue;if(+k[j].high>=+k[i].high)H=0;if(+k[j].low<=+k[i].low)L=0}if(H)out.push({index:i,pivotIndex:i,type:'H',price:+k[i].high,confirmedAt:i+right});if(L)out.push({index:i,pivotIndex:i,type:'L',price:+k[i].low,confirmedAt:i+right})}return out.sort((a,b)=>a.confirmedAt-b.confirmedAt||a.index-b.index)}
function canonical(raw,asOf){const out=[];for(const p of raw){if(p.confirmedAt>asOf)break;const s={...p,confirmed:true,status:'active'};if(!out.length||out.at(-1).type!==s.type){out.push(s);continue}const q=out.at(-1),more=s.type==='H'?s.price>q.price:s.price<q.price;if(more)out[out.length-1]=s}return out}
function atr(k,p=14){const tr=k.map((x,i)=>i?Math.max(+x.high-+x.low,Math.abs(+x.high-+k[i-1].close),Math.abs(+x.low-+k[i-1].close)):+x.high-+x.low),o=[];let v=tr[0]||0;const a=2/(p+1);for(let i=0;i<tr.length;i++){v=i?tr[i]*a+v*(1-a):tr[i];o.push(v)}return o}
function ema(v,p){const a=2/(p+1),o=[];let x=v[0]||0;for(let i=0;i<v.length;i++){x=i?v[i]*a+x*(1-a):v[i];o.push(x)}return o}
function qtile(a,q){if(!a.length)return null;const b=[...a].sort((x,y)=>x-y),x=(b.length-1)*q,l=Math.floor(x),h=Math.ceil(x);return l===h?b[l]:b[l]+(b[h]-b[l])*(x-l)}
function seeded(seed=1234567){let x=seed>>>0;return()=>{x=(1664525*x+1013904223)>>>0;return x/4294967296}}
function blockBootstrap(values,iterations=160,block=5){if(values.length<8)return{lo:null,hi:null};const rnd=seeded(values.length*7919+17),stats=[];for(let r=0;r<iterations;r++){const s=[];while(s.length<values.length){const st=Math.floor(rnd()*values.length);for(let j=0;j<block&&s.length<values.length;j++)s.push(values[(st+j)%values.length])}stats.push(med(s))}return{lo:qtile(stats,.05),hi:qtile(stats,.95)}}
function splitBounds(n,warmup,horizon){const start=warmup,end=n-horizon-1,usable=Math.max(1,end-start),devEnd=start+Math.floor(usable*.55),validEnd=start+Math.floor(usable*.75);return{start,devEnd,validEnd,end}}
function splitOf(i,b){return i<b.devEnd?'dev':i<b.validEnd?'valid':'holdout'}
function regimeAt(i,k,aa,e20,e50){const a=aa[i]||0,c=Math.abs(+k[i].close)||1,vol=a/c,arr=[];for(let z=Math.max(20,i-100);z<i;z++)arr.push((aa[z]||0)/(Math.abs(+k[z].close)||1));const hi=vol>(med(arr)||vol),trend=a?Math.abs(e20[i]-e50[i])/a>.75:false,trendDir=e20[i]>e50[i]?'up':'down';return{regime:(trend?'trend':'range')+'_'+(hi?'high_vol':'low_vol'),trendDirection:trend?trendDir:'flat',atrPct:vol*100}}
function outcome(i,k,horizon,dir){const entry=+k[i].close,end=+k[i+horizon].close;if(!entry||!end)return null;let hi=-Infinity,lo=Infinity;for(let z=i+1;z<=i+horizon;z++){hi=Math.max(hi,+k[z].high);lo=Math.min(lo,+k[z].low)}const raw=(end/entry-1)*100;if(dir===0)return{rawRet:raw,dirRet:Math.abs(raw),mfe:Math.max(Math.abs(hi/entry-1),Math.abs(lo/entry-1))*100,mae:0};if(dir>0)return{rawRet:raw,dirRet:raw,mfe:(hi/entry-1)*100,mae:(lo/entry-1)*100};return{rawRet:raw,dirRet:-raw,mfe:(1-lo/entry)*100,mae:(1-hi/entry)*100}}
function summarize(a){if(!a.length)return{samples:0};const edge=a.map(x=>x.edge40).filter(Number.isFinite),ci=blockBootstrap(edge);return{samples:a.length,median40:med(a.map(x=>x.rawRet)),medianDirectional40:med(a.map(x=>x.dirRet)),positiveDirectional40:a.filter(x=>x.dirRet>0).length/a.length*100,baselineDirectional40:med(a.map(x=>x.baseline40)),edge40:med(edge),edgeCi90:ci,medianMfe40:med(a.map(x=>x.mfe)),medianMae40:med(a.map(x=>x.mae)),avgConfidence:avg(a.map(x=>x.confidence)),regimes:Object.fromEntries([...new Set(a.map(x=>x.regime))].map(r=>{const z=a.filter(x=>x.regime===r);return[r,{samples:z.length,edge40:med(z.map(x=>x.edge40))}]}))}}
function walkForward(a,bounds,windows=6){const span=bounds.devEnd-bounds.start,out=[];for(let w=0;w<windows;w++){const a0=bounds.start+Math.floor(span*w/windows),b0=bounds.start+Math.floor(span*(w+1)/windows),x=a.filter(e=>e.i>=a0&&e.i<b0),s=summarize(x);out.push({window:w+1,samples:s.samples||0,edge40:s.edge40??null})}const valid=out.filter(x=>x.samples>=3&&Number.isFinite(x.edge40)),positive=valid.filter(x=>x.edge40>0).length,ratios=[];for(let i=1;i<valid.length;i+=2){const is=Math.abs(valid[i-1].edge40),os=Math.abs(valid[i].edge40);if(is>.05&&Math.sign(valid[i-1].edge40)===Math.sign(valid[i].edge40))ratios.push(Math.min(1.5,os/is))}return{windows:out,positiveWindowRatio:valid.length?positive/valid.length*100:null,oosRetention:ratios.length?med(ratios)*100:null}}
function reliability(valid,dev,wf){const s=valid.samples||0,sample=20*Math.min(1,s/50),edge=25*clamp((valid.edge40||0)/1.5,0,1),mfe=Number.isFinite(valid.medianMfe40)&&Number.isFinite(valid.medianMae40)?20*clamp(valid.medianMfe40/(valid.medianMfe40+Math.abs(valid.medianMae40)+1e-9),0,1):0,stable=25*((wf.positiveWindowRatio||0)/100),ci=valid.edgeCi90||{},cal=10*(Number.isFinite(ci.lo)?(ci.lo>0?1:ci.hi>0?.5:0):.25);return Math.round(clamp(sample+edge+mfe+stable+cal))}
function recommendation(type,score,valid,dev,wf){const samples=valid.samples||0,tunables=tunablesFor(type);let status='COLLECT',confidenceDelta=0,note='표본 부족';if(samples>=15){if(score>=80){status='DEFAULT_ON';confidenceDelta=-1;note='VALID 안정성 양호'}else if(score>=60){status='OBSERVE';note='기본 표시 가능, 자동 완화 금지'}else if(score>=40){status='RESEARCH_ONLY';confidenceDelta=2;note='연구 화면 유지'}else{status='DISABLE_CANDIDATE';confidenceDelta=4;note='오탐/불안정 가능성 점검'}}if((wf.positiveWindowRatio||0)<50&&status!=='COLLECT'){status='RESEARCH_ONLY';confidenceDelta=Math.max(confidenceDelta,2);note='Walk-forward 윈도우 일관성 부족'}return{status,reliabilityScore:score,proposed:{confidenceDelta},tunables,note,autoApply:false}}
function validatePatterns({klines,interval='4h',warmup=180,horizon=40,step=4,includeHoldout=false}){const raw=pivots(klines),aa=atr(klines),closes=klines.map(x=>+x.close),e20=ema(closes,20),e50=ema(closes,50),bounds=splitBounds(klines.length,warmup,horizon),events=[],baseline=[],seen=new Map();for(let i=bounds.start;i<bounds.end;i+=step){const split=splitOf(i,bounds),reg=regimeAt(i,klines,aa,e20,e50);for(const dir of [-1,0,1]){const o=outcome(i,klines,horizon,dir);if(o)baseline.push({i,split,dir,regime:reg.regime,...o})}const sw=canonical(raw,i),slice=klines.slice(0,i+1),base=detectChartPatterns({klines:slice,swings:sw,atrByIndex:aa.slice(0,i+1),interval}),fw=analyzeFallingWedge({klines:slice,swings:sw,atrByIndex:aa.slice(0,i+1),interval});const pats=[...base];if(fw&&fw.state!=='FAILED')pats.push({type:'falling_wedge',confidence:fw.confidence,state:fw.state,anchors:[],meta:fw.meta});for(const p of pats){const type=p.type||p.pattern,dir=DIR[type]??0,anchors=p.anchors||[],lastAnchor=anchors.length?Math.max(...anchors.map(a=>Number(a.index??a.pivotIndex)).filter(Number.isFinite)):Number(p.meta?.commonEndIndex??i),key=type+':'+lastAnchor,old=seen.get(key);if(old&&i-old.lastSeen<Math.max(8,step*2)){old.lastSeen=i;continue}seen.set(key,{firstSeen:i,lastSeen:i});const o=outcome(i,klines,horizon,dir);if(!o)continue;const anchorConfirmed=anchors.map(a=>sw.find(s=>s.index===Number(a.index??a.pivotIndex)&&String(s.type).startsWith(String(a.type||'')[0]))?.confirmedAt).filter(Number.isFinite),confirmedIndex=anchorConfirmed.length?Math.max(...anchorConfirmed):Math.min(i,lastAnchor+3);events.push({type,i,time:klines[i].time,split,direction:dir,confidence:p.confidence,state:p.state||'ACTIVE',firstSeenAt:klines[i].time,detectedAt:klines[i].time,confirmedAt:klines[confirmedIndex]?.time||klines[i].time,confirmedIndex,regime:reg.regime,trendDirection:reg.trendDirection,atrPct:reg.atrPct,...o})}}
const baselineKey={};for(const x of baseline){const k=x.split+'|'+x.dir+'|'+x.regime;(baselineKey[k]||(baselineKey[k]=[])).push(x.dirRet)}for(const e of events){const k=e.split+'|'+e.direction+'|'+e.regime,k2=e.split+'|'+e.direction+'|all',a=baselineKey[k]||baseline.filter(x=>x.split===e.split&&x.dir===e.direction).map(x=>x.dirRet);e.baseline40=med(a)||0;e.edge40=e.dirRet-e.baseline40}
const types=[...new Set(events.map(x=>x.type))],byType=[];for(const type of types){const all=events.filter(x=>x.type===type),dev=summarize(all.filter(x=>x.split==='dev')),valid=summarize(all.filter(x=>x.split==='valid')),holdout=summarize(all.filter(x=>x.split==='holdout')),wf=walkForward(all.filter(x=>x.split==='dev'),bounds),score=reliability(valid,dev,wf),calibration=recommendation(type,score,valid,dev,wf);byType.push({type,dev,valid,holdout:includeHoldout?holdout:{locked:true,samples:hiddenCount(all.filter(x=>x.split==='holdout').length)},walkForward:wf,calibration})}
function hiddenCount(n){return n?'>0':'0'}
byType.sort((a,b)=>(b.calibration.reliabilityScore||0)-(a.calibration.reliabilityScore||0));const visible=events.filter(e=>includeHoldout||e.split!=='holdout');return{events:visible,summary:byType.map(x=>({type:x.type,samples:(x.valid.samples||0),median40:x.valid.median40,positive40:x.valid.positiveDirectional40,medianMfe40:x.valid.medianMfe40,medianMae40:x.valid.medianMae40,edge40:x.valid.edge40,reliabilityScore:x.calibration.reliabilityScore,status:x.calibration.status})),calibration:byType,dataset:{ratios:{dev:.55,valid:.20,holdout:.25},bounds:{dev:[bounds.start,bounds.devEnd-1],valid:[bounds.devEnd,bounds.validEnd-1],holdout:[bounds.validEnd,bounds.end-1]},holdoutLocked:!includeHoldout},versions:{patternEngineVersion:ENGINE_VERSION,calibrationSetVersion:CALIBRATION_VERSION},audit:{pointInTime:true,futurePivotUse:0,horizon,step,warmup,holdoutIncluded:includeHoldout,freeParameterLimit:3,baseline:'same split + direction + regime median',bootstrap:'deterministic moving-block 90% CI',walkForwardWindows:6},note:'Historical detector diagnostics only; no execution simulation or future probability.'}}
module.exports={DIR,validatePatterns};