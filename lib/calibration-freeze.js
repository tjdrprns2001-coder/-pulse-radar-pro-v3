const crypto=require('crypto');
const {ENGINE_VERSION,CALIBRATION_VERSION,SPLIT_RATIOS,RELIABILITY_BANDS,GLOBAL,TF_DELTA,RULES,TUNABLES,PARAM_META,MIN_SAMPLES_PER_FREE_PARAMETER,MIN_VALID_SAMPLES}=require('./calibration-profile.js');
function stable(v){if(Array.isArray(v))return v.map(stable);if(v&&typeof v==='object'){const o={};for(const k of Object.keys(v).sort())o[k]=stable(v[k]);return o}return v}
function buildFreezeManifest(){const manifest=stable({schema:'pulse-calibration-freeze-v1',engineVersion:ENGINE_VERSION,calibrationVersion:CALIBRATION_VERSION,splitRatios:SPLIT_RATIOS,reliabilityBands:RELIABILITY_BANDS,samplePolicy:{minSamplesPerFreeParameter:MIN_SAMPLES_PER_FREE_PARAMETER,minValidSamples:MIN_VALID_SAMPLES,maxFreeParametersPerFamily:3},configuration:{global:GLOBAL,timeframeDelta:TF_DELTA,rules:RULES,tunables:TUNABLES,paramMeta:PARAM_META},evaluation:{horizons:[10,20,40],regimes:['trend_high_vol','trend_low_vol','range_high_vol','range_low_vol'],walkForwardWindows:6,bootstrap:'deterministic moving-block 90%',holdout:'version-pinned and hidden during tuning'},policy:{autoApply:false,holdoutTuning:false,pointInTimeRequired:true}});const canonical=JSON.stringify(manifest),sha256=crypto.createHash('sha256').update(canonical).digest('hex');return{manifest,sha256,canonicalBytes:Buffer.byteLength(canonical)}}
module.exports={buildFreezeManifest};
