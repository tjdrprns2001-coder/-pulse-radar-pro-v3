const test=require('node:test');const assert=require('node:assert/strict');const {getModeDefinition,listModes,getRolePreset}=require('../ui/multi-chart/mode-registry');
test('supports liquidity as an explicit mode without removing existing modes',()=>{assert.deepEqual(listModes(),['clean','structure','smc','liquidity','ict','dante','full'])});
test('SMC Liquidity and ICT are visually and behaviorally distinct',()=>{const s=getModeDefinition('smc','desktop'),l=getModeDefinition('liquidity','desktop'),i=getModeDefinition('ict','desktop');assert.notDeepEqual(s.plugins,l.plugins);assert.notDeepEqual(l.plugins,i.plugins);assert.equal(l.visualProfile,'liquidity');assert.deepEqual(l.plugins,['liquidity']);assert.ok(s.plugins.includes('smc'));assert.ok(i.plugins.includes('ict'))});
test('Clean has no analytical overlays and Dante focuses Dante plugin',()=>{assert.deepEqual(getModeDefinition('clean','desktop').plugins,[]);assert.deepEqual(getModeDefinition('dante','desktop').plugins,['dante'])});
test('Full is curated instead of unioning every analytical plugin',()=>{const f=getModeDefinition('full','desktop');assert.ok(f.plugins.length>0);assert.ok(f.plugins.length<=4);assert.ok(f.plugins.includes('liquidity'));assert.ok(!f.plugins.includes('ict')||!f.plugins.includes('smc')||f.plugins.length<=3)});
test('publishes approved two-chart and four-chart context role presets',()=>{assert.deepEqual(getRolePreset(2),[{timeframe:'1h',mode:'liquidity'},{timeframe:'4h',mode:'ict'}]);assert.deepEqual(getRolePreset(4),[{timeframe:'15m',mode:'liquidity'},{timeframe:'1h',mode:'smc'},{timeframe:'4h',mode:'ict'},{timeframe:'1d',mode:'structure'}])});