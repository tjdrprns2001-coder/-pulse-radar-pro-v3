(function(root,factory){const api=factory(root?.PulseForexBookEngine);if(typeof module==='object'&&module.exports)module.exports=api;else root.PulseForexBookPlugin=api})(typeof globalThis!=='undefined'?globalThis:this,function(E){'use strict';
function createForexBookPlugin(){let ctx=null,visible=true,series=[],markers=null;const sec=v=>Math.trunc(Number(v)>1e12?Number(v)/1000:Number(v));function clear(){for(const s of series){try{ctx?.chart?.removeSeries(s)}catch{}}series=[];try{markers?.setMarkers([]);markers?.detach?.()}catch{}markers=null}function line(price,candles,style=2){if(!ctx||!Number.isFinite(Number(price))||!candles?.length)return;const s=ctx.addLineSeries({lineWidth:1,lineStyle:style,lastValueVisible:false,priceLineVisible:false});s.setData([{time:sec(candles[Math.max(0,candles.length-100)].time),value:Number(price)},{time:sec(candles.at(-1).time),value:Number(price)}]);series.push(s)}
return{id:'forex-book',version:'1.0.0',requiredData:['rawCandles'],mount(c){ctx=c},update(state){clear();if(!ctx||!visible||!E)return;const candles=state?.rawCandles||[];if(!candles.length)return;const a=state?.forexBook||E.analyze({candles,smc:state?.smc,liquidity:state?.liquidity,ictContext:state?.ictContext}),ind=a?.indicators||{};line(ind.supportResistance?.support,candles,2);line(ind.supportResistance?.resistance,candles,2);const fib=ind.fibonacci?.levels||{};line(fib['38.2'],candles,3);line(fib['61.8'],candles,3);const ms=[];for(const p of (a?.patterns||[]).slice(0,4)){const c=candles[p.index];if(!c)continue;ms.push({time:sec(c.time),position:p.side==='하락'?'aboveBar':'belowBar',shape:p.side==='하락'?'arrowDown':'arrowUp',text:'FX '+p.label,color:p.side==='하락'?'#ff7b88':'#62dba7'})}if(ms.length&&ctx.library?.createSeriesMarkers){try{markers=ctx.library.createSeriesMarkers(ctx.candlesSeries,ms,{autoScale:false})}catch{}}},setVisible(v){visible=!!v;if(!visible)clear()},dispose(){clear();ctx=null}}}
return{createForexBookPlugin};
});