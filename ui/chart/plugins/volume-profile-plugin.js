(function(root,factory){const api=factory();if(typeof module==='object'&&module.exports)module.exports=api;else root.PulseVolumeProfilePlugin=api;})(typeof globalThis!=='undefined'?globalThis:this,function(){'use strict';
function createVolumeProfilePlugin(){let ctx=null,visible=true,canvas=null,state=null,ro=null;function cleanup(){try{ro?.disconnect()}catch{}ro=null;if(canvas?.parentNode)canvas.parentNode.removeChild(canvas);canvas=null}function ensure(){if(canvas||!ctx?.container)return;const c=ctx.container;c.style.position=c.style.position||'relative';canvas=document.createElement('canvas');canvas.style.cssText='position:absolute;inset:0;pointer-events:none;z-index:3;width:100%;height:100%';c.appendChild(canvas);if(typeof ResizeObserver!=='undefined'){ro=new ResizeObserver(draw);ro.observe(c)}}function draw(){if(!visible||!canvas||!ctx||!state)return;const dpr=Math.max(1,window.devicePixelRatio||1),w=ctx.container.clientWidth,h=ctx.container.clientHeight;if(!w||!h)return;canvas.width=Math.round(w*dpr);canvas.height=Math.round(h*dpr);const g=canvas.getContext('2d');g.setTransform(dpr,0,0,dpr,0,0);g.clearRect(0,0,w,h);const p=state.technical?.volumeProfile;if(!p?.bins?.length||!ctx.candlesSeries?.priceToCoordinate)return;const max=Math.max(...p.bins.map(x=>Number(x.volume)||0),1),maxW=Math.min(150,w*.24),right=w-4;g.globalAlpha=.28;for(const b of p.bins){const y=ctx.candlesSeries.priceToCoordinate(Number(b.mid));if(!Number.isFinite(y))continue;const bw=(Number(b.volume)||0)/max*maxW;g.fillStyle=p.poc&&Math.abs(Number(b.mid)-Number(p.poc.price))<Math.abs(Number(b.to)-Number(b.from))/2?'#ffc45f':'#5a8fff';g.fillRect(right-bw,y-2,bw,4)}g.globalAlpha=1;if(p.poc){const y=ctx.candlesSeries.priceToCoordinate(Number(p.poc.price));if(Number.isFinite(y)){g.strokeStyle='#ffc45f';g.setLineDash([4,4]);g.beginPath();g.moveTo(right-maxW,y);g.lineTo(right,y);g.stroke();g.setLineDash([]);g.fillStyle='#ffc45f';g.font='10px system-ui';g.fillText('POC',Math.max(4,right-maxW),Math.max(12,y-4))}}}
return{id:'volume-profile',version:'1.0.0',requiredData:['technical'],mount(c){ctx=c;ensure()},update(s){state=s;ensure();draw()},setVisible(v){visible=!!v;if(visible){ensure();draw()}else if(canvas){const g=canvas.getContext('2d');g.clearRect(0,0,canvas.width,canvas.height)}},dispose(){cleanup();ctx=null;state=null}}}
return{createVolumeProfilePlugin};
});