(()=>{'use strict';
const $=id=>document.getElementById(id);const state={category:'전체',sector:'전체',query:'',items:[],meta:null,loading:false,error:null};
function escapeHtml(v){return String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]))}
function fmtTime(v){const n=Number(v);if(!Number.isFinite(n))return'-';try{return new Date(n).toLocaleString('ko-KR',{hour12:false})}catch{return'-'}}
function matchesCategory(item){const c=state.category;if(c==='전체')return true;if(c==='급등 전조')return String(item.category).startsWith('급등 전조');return item.category===c}
function filtered(){const q=state.query.trim().toUpperCase();return state.items.filter(x=>matchesCategory(x)&&(state.sector==='전체'||x.sector===state.sector)&&(!q||String(x.symbol).includes(q)))}
function renderMeta(){const m=state.meta||{},h=m.dataHealth||{};$('updatedAt').textContent=fmtTime(m.updatedAt);$('scanCount').textContent=String(m.scanCount||0);$('deepCount').textContent=String(m.deepScanCount||0);$('liveCount').textContent=String(h.live||0);$('delayedCount').textContent=String(h.delayed||0);$('blockedCount').textContent=String(h.blocked||0);$('status').textContent=state.loading?'자동 스캔 중…':state.error?`데이터 갱신 실패 · 이전 결과 유지 · ${state.error}`:`${filtered().length}개 표시 · 60초마다 자동 갱신`}
function card(x){const reasons=(Array.isArray(x.reasons)?x.reasons:[]).slice(0,3).map(r=>`<li>${escapeHtml(r)}</li>`).join('');const tf=x.tfState||{};const href=`/snapshot-analysis-restored.html?symbol=${encodeURIComponent(x.symbol||'')}`;return `<article class="scanCard"><div class="cardTop"><div><div class="symbol">${escapeHtml(x.symbol)}</div><div class="meta">${escapeHtml(x.sector||'기타')} · 우선순위 ${escapeHtml(x.priority??0)} · 후보점수 ${escapeHtml(x.candidateScore??'-')}</div></div><span class="category">${escapeHtml(x.category)}</span></div><div class="tf">4H ${escapeHtml(tf['4h']||'-')} · 1H ${escapeHtml(tf['1h']||'-')} · 15m ${escapeHtml(tf['15m']||'-')}</div>${reasons?`<ul class="reason">${reasons}</ul>`:''}<div class="summary">${escapeHtml(x.summary||'요약 정보 없음')}</div><div class="cardFoot"><span class="state ${escapeHtml(x.dataState)}">● ${escapeHtml(x.dataState||'unknown')}</span><a class="detail" href="${href}">스냅샷 분석</a></div></article>`}
function render(){renderMeta();const rows=filtered();$('results').innerHTML=rows.length?rows.map(card).join(''):'<div class="empty">현재 조건에 맞는 종목이 없습니다.</div>'}
async function refresh(){if(state.loading)return;state.loading=true;state.error=null;renderMeta();try{const r=await fetch('/api/coin-scan?mode=summary&limit=250',{cache:'no-store'});if(!r.ok)throw new Error(`HTTP ${r.status}`);const data=await r.json();if(!data||data.status!=='ok'||!Array.isArray(data.items))throw new Error(data?.error||'응답 형식 오류');state.items=data.items;state.meta=data}catch(e){state.error=String(e?.message||e)}finally{state.loading=false;render()}}
function init(){document.querySelectorAll('#categoryTabs button').forEach(btn=>btn.addEventListener('click',()=>{document.querySelectorAll('#categoryTabs button').forEach(x=>x.classList.remove('active'));btn.classList.add('active');state.category=btn.dataset.category||'전체';render()}));$('sector').addEventListener('change',e=>{state.sector=e.target.value;render()});$('query').addEventListener('input',e=>{state.query=e.target.value;render()});$('refreshBtn').addEventListener('click',refresh);refresh();setInterval(refresh,60000)}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',init);else init();
})();
