const assert=require('assert');const {createCoinReportService}=require('../lib/coin-report/service.js');const handler=require('../api/coin-report.js');function frame(){const a=[];for(let i=0;i<80;i++){const c=10+i*.03;a.push([i,c-.01,c+.05,c-.05,c,100,0,1000,0,0,550])}return a}(async()=>{const provider={async getKlines(s,tf){if(tf==='1w')throw new Error('partial');return frame()},async getDerivativesContext(){return{oiChangePct:1,fundingPct:.01}}};const gateway={available:false};const service=createCoinReportService({provider,gateway,now:()=>123});const r=await service.getReport('AAAUSDT');assert.equal(r.symbol,'AAAUSDT');assert.equal(r.updatedAt,123);assert(r.dataWarnings.some(x=>x.includes('1w')));const news=await service.getNews('AAAUSDT',r);assert.equal(news.available,false);let code,body;const res={status(n){code=n;return this},json(v){body=v;return v}};await handler({method:'GET',query:{symbol:'AAAUSDT'}},res,{service});assert.equal(code,200);assert.equal(body.status,'ok');await handler({method:'GET',query:{symbol:'BAD'}},res,{service});assert.equal(code,400);console.log('coin report api PASS')})().catch(e=>{console.error(e);process.exit(1)});