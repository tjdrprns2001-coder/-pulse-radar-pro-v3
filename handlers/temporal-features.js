const BASES=['https://data-api.binance.vision','https://api.binance.com','https://api-gcp.binance.com'];
const n=(v,f=0)=>{v=Number(v);return Number.isFinite(v)?v:f};
async function getJson(path,params={}){let last;const qs=new URLSearchParams(params).toString();for(const base of BASES){try{const r=await fetch(base+path+(qs?'?'+qs:''),{headers:{'User-Agent':'PulseRadar-Pro/3.10D'}});const t=await r.text();if(!r.ok)throw Error(`Binance ${r.status}`);return JSON.parse(t)}catch(e){last=e}}throw last||Error('Binance failed')}
function ema(a,p){const k=2/(p+1);let x=a[0]||0;return a.map((v,i)=>(x=i?v*k+x*(1-k):v))}
function rsi(a,p=14){let g=0,l=0;if(a.length<=p)return 50;for(let i=1;i<=p;i++){const d=a[i]-a[i-1];g+=Math.max(d,0);l+=Math.max(-d,0)}g/=p;l/=p;let r=l?100-100/(1+g/l):100;for(let i=p+1;i<a.length;i++){const d=a[i]-a[i-1];g=(g*(p-1)+Math.max(d,0))/p;l=(l*(p-1)+Math.max(-d,0))/p;r=l?100-100/(1+g/l):100}return r}
function sd(a){const m=a.reduce((s,x)=>s+x,0)/Math.max(1,a.length);return Math.sqrt(a.reduce((s,x)=>s+(x-m)**2,0)/Math.max(1,a.length))}
function frame(raw){const c=raw.map(k=>n(k[4])),price=c.at(-1),e5=ema(c,5).at(-1),e20=ema(c,20).at(-1),e60=ema(c,60).at(-1),gap=(Math.max(e5,e20,e60)-Math.min(e5,e20,e60))/Math.max(e5,e20,e60)*100,rr=rsi(c),w=c.slice(-20),mid=w.reduce((s,x)=>s+x,0)/Math.max(1,w.length),bbw=mid?4*sd(w)/mid*100:0;return{ema:100/(1+gap),bb:100/(1+bbw),rsi:Math.max(0,100-Math.abs(rr-55)*4),trend:price>e5&&e5>e20&&e20>e60}}
function target(fs){const avg=k=>fs.reduce((s,x)=>s+x[k],0)/fs.length,trend=fs.filter(x=>x.trend).length/3*100;return Math.max(0,Math.min(100,.35*avg('rsi')+.30*avg('ema')+.25*avg('bb')+.10*trend))}
module.exports=async function handler(req,res){res.setHeader('Cache-Control','s-maxage=60, stale-while-revalidate=120');try{const symbols=String(req.query.symbols||'').split(',').map(x=>x.trim().toUpperCase()).filter(Boolean).slice(0,10),hours=Math.max(24,Math.min(720,n(req.query.hours,168)));if(!symbols.length)return res.status(400).json({ok:false,error:'symbols required'});const end=Date.now()-hours*3600000,rows=await Promise.all(symbols.map(async symbol=>{try{const tfs=['5m','15m','1h'],raws=await Promise.all(tfs.map(interval=>getJson('/api/v3/klines',{symbol,interval,endTime:String(end),limit:'120'}))),fs=raws.map(frame),avg=k=>fs.reduce((s,x)=>s+x[k],0)/3;return{ok:true,symbol,micro:{emaConvergence:avg('ema'),bbCompression:avg('bb'),rsiSetup:avg('rsi')},proxyScore:target(fs)}}catch(e){return{ok:false,symbol,error:e.message}}}));res.status(200).json({ok:true,version:'3.10D',snapshotHoursAgo:hours,snapshotAt:new Date(end).toISOString(),rows})}catch(e){res.status(502).json({ok:false,error:e.message})}}
