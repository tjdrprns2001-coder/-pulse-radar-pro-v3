const BASES=['https://data-api.binance.vision','https://api.binance.com','https://api-gcp.binance.com'];
const n=(v,f=0)=>{v=Number(v);return Number.isFinite(v)?v:f};
async function getJson(path,params={}){let last;const qs=new URLSearchParams(params).toString();for(const base of BASES){try{const r=await fetch(base+path+(qs?'?'+qs:''),{headers:{'User-Agent':'PulseRadar-Pro/3.10B'}});const t=await r.text();if(!r.ok)throw new Error(`Binance ${r.status}`);return JSON.parse(t)}catch(e){last=e}}throw last||new Error('Binance failed')}
function ema(a,p){const k=2/(p+1);let x=a[0]||0;return a.map((v,i)=>(x=i?v*k+x*(1-k):v))}
function sma(a,p){let s=0,o=Array(a.length).fill(null);for(let i=0;i<a.length;i++){s+=a[i];if(i>=p)s-=a[i-p];if(i>=p-1)o[i]=s/p}return o}
function rsi(a,p=14){let g=0,l=0;if(a.length<=p)return 50;for(let i=1;i<=p;i++){const d=a[i]-a[i-1];g+=Math.max(d,0);l+=Math.max(-d,0)}g/=p;l/=p;let r=l?100-100/(1+g/l):100;for(let i=p+1;i<a.length;i++){const d=a[i]-a[i-1];g=(g*(p-1)+Math.max(d,0))/p;l=(l*(p-1)+Math.max(-d,0))/p;r=l?100-100/(1+g/l):100}return r}
function sd(a){const m=a.reduce((s,x)=>s+x,0)/Math.max(1,a.length);return Math.sqrt(a.reduce((s,x)=>s+(x-m)**2,0)/Math.max(1,a.length))}
function frame(raw,tf){const closed=raw.filter(k=>n(k[6])<Date.now()),b=closed.length>=80?closed:raw.slice(0,-1),c=b.map(k=>n(k[4])),v=b.map(k=>n(k[5]));const price=c.at(-1),e5=ema(c,5).at(-1),e20=ema(c,20).at(-1),e60=ema(c,60).at(-1),maxE=Math.max(e5,e20,e60),minE=Math.min(e5,e20,e60),gap=maxE?(maxE-minE)/maxE*100:0,vs=sma(v,20).at(-1)||1,volRatio=v.at(-1)/vs,rr=rsi(c),w=c.slice(-20),mid=w.reduce((s,x)=>s+x,0)/Math.max(1,w.length),bbWidth=mid?4*sd(w)/mid*100:0,buy=b.slice(-5).reduce((s,k)=>s+n(k[10]),0),q=b.slice(-5).reduce((s,k)=>s+n(k[7]),0),buyRatio=q?buy/q:.5;return{tf,emaGapPct:gap,emaConvergence:100/(1+gap),volRatio,rsi14:rr,rsiSetup:Math.max(0,100-Math.abs(rr-55)*4),bbWidthPct:bbWidth,bbCompression:100/(1+bbWidth),buyRatio,buyStrength:Math.max(0,Math.min(100,(buyRatio-.45)/.15*100)),trendAligned:price>e5&&e5>e20&&e20>e60}}
module.exports=async function handler(req,res){res.setHeader('Cache-Control','s-maxage=15, stale-while-revalidate=30');try{const symbols=String(req.query.symbols||'').split(',').map(x=>x.trim().toUpperCase()).filter(Boolean).slice(0,12);if(!symbols.length)return res.status(400).json({ok:false,error:'symbols required'});const rows=await Promise.all(symbols.map(async symbol=>{try{const tfs=['5m','15m','1h'];const raws=await Promise.all(tfs.map(interval=>getJson('/api/v3/klines',{symbol,interval,limit:'120'})));const f=raws.map((r,i)=>frame(r,tfs[i])),avg=k=>f.reduce((s,x)=>s+n(x[k]),0)/f.length;return{ok:true,symbol,micro:{emaConvergence:avg('emaConvergence'),volumeRatio:avg('volRatio'),rsiSetup:avg('rsiSetup'),bbCompression:avg('bbCompression'),buyStrength:avg('buyStrength'),mtfTrend:f.filter(x=>x.trendAligned).length/3*100},frames:f}}catch(e){return{ok:false,symbol,error:e.message}}}));res.status(200).json({ok:true,version:'3.10B',rows})}catch(e){res.status(502).json({ok:false,error:e?.message||'micro feature failed'})}}
