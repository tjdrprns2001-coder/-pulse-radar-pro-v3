name: Diagnose Signal Performance Production
on:
  pull_request:
    branches: [main]
jobs:
  diagnose:
    runs-on: ubuntu-latest
    steps:
      - name: Fetch production endpoint
        run: |
          set +e
          echo '=== RESPONSE ==='
          curl -sS -i 'https://pulseradar-pro-v3-temp.netlify.app/api/signal-performance'
          echo
          echo '=== DONE ==='
