name: Check Book AI JS
on:
  push:
    branches: [fix/book-ai-js-syntax]
jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: node --check ui/book-ai/app.js
