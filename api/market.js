const spot=require('../lib/spot-market.js');
const futures=require('../lib/futures-data.js');
function capture(){let code=200,body=null;return{res:{setHeader(){},status(c){code=c;return this},json(v){body=v;return this}},get:()=>({code,body})}}
async function runSpot(req){const c=capture();try{await spot(req,c.res)}catch(e){return{code:500,body:{ok:false,error:e?.message||String(e)}}}return c.get()}
module.exports=async function handler(req,res){res.setHeader('Cache-Control','s-maxage=20, stale-while-revalidate=40');try{const min=Number(req.query.minVolume)||500000;const [s,f]=await Promise.all([runSpot(req),futures.market(min).catch(e=>({ok:false,error:e.message}))]);const sr=s.body?.ok?(s.body.results||[]).map(x=>({...x,market:'spot'})):[];const spotSymbols=new Set(sr.map(x=>x.symbol));const fr=f?.ok?(f.results||[]).filter(x=>!spotSymbols.has(x.symbol)):[];const rows=[...sr,...fr];rows.sort((a,b)=>Math.max(Number(b.surgeScore)||0,Number(b.preScore)||0)-Math.max(Number(a.surgeScore)||0,Number(a.preScore)||0)||Number(b.quoteVol24||0)-Number(a.quoteVol24||0));return res.status(200).json({ok:true,version:'spot+futures-auto-v1',market:'spot+futures',updatedAt:new Date().toISOString(),total:rows.length,eligible:rows.filter(x=>x.pass).length,results:rows,spotCount:sr.length,futuresOnlyCount:fr.length,note:'현물 우선 + 현물에 없는 Binance USD-M 무기한 선물 종목'});}catch(e){return res.status(502).json({ok:false,error:e?.message||'Data fetch failed'})}}
