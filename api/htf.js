const BASES=['https://api.binance.com','https://api-gcp.binance.com','https://data-api.binance.vision'];
const num=(v,f=0)=>{const x=Number(v);return Number.isFinite(x)?x:f};
const HIGHER={'5m':'15m','15m':'1h','1h':'4h','4h':'1d','1d':'1w'};
async function getJson(path,params={}){const qs=new URLSearchParams(params).toString();let last=null;for(const base of BASES){const ac=new AbortController(),timer=setTimeout(()=>ac.abort(),3500);try{const r=await fetch(base+path+(qs?'?'+qs:''),{signal:ac.signal,headers:{'User-Agent':'PulseRadar-Pro/3.12.5-HTF'}});const text=await r.text();if(!r.ok)throw Error('Binance '+r.status+': '+text.slice(0,120));return JSON.parse(text)}catch(e){last=e}finally{clearTimeout(timer)}}throw last||Error('Binance request failed')}
function ema(a,p){if(!a.length)return[];const k=2/(p+1),out=[];let x=a[0];for(let i=0;i<a.length;i++){x=i?a[i]*k+x*(1-k):a[i];out.push(x)}return out}
module.exports=async function handler(req,res){res.setHeader('Cache-Control','s-maxage=60, stale-while-revalidate=120');try{const symbol=String(req.query.symbol||'BTCUSDT').toUpperCase(),baseInterval=String(req.query.interval||'15m'),interval=HIGHER[baseInterval];if(!interval)return res.status(200).json({ok:true,version:'3.12.5',status:'unknown',bias:'unknown',reason:'No higher timeframe mapping'});const raw=await getJson('/api/v3/klines',{symbol,interval,limit:'180'});if(!Array.isArray(raw))throw Error('Unexpected Binance response');const snapshotTime=num(req.query.snapshotTime,Date.now()),closed=raw.filter(k=>num(k[6])<=snapshotTime);if(closed.length<70)return res.status(200).json({ok:true,version:'3.12.5',status:'unknown',bias:'unknown',interval,reason:'Not enough closed HTF candles',snapshotTime});const c=closed.map(k=>num(k[4])),e20=ema(c,20),e60=ema(c,60),price=c[c.length-1],a=e20[e20.length-1],b=e60[e60.length-1],bias=price>a&&a>b?'bullish':price<a&&a<b?'bearish':'neutral',last=closed[closed.length-1];return res.status(200).json({ok:true,version:'3.12.5',status:'pass',bias,interval,price,ema20:a,ema60:b,snapshotTime,lastClosedOpenTime:num(last[0]),lastClosedCloseTime:num(last[6]),currentCandleExcluded:true,dataSource:'Binance Spot',boundaryPass:num(last[6])<=snapshotTime})}catch(e){return res.status(200).json({ok:true,version:'3.12.5',status:'unknown',bias:'unknown',reason:e?.message||String(e)})}}
